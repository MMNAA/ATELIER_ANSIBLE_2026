---
- name: Atelier Ansible
  hosts: all
  become: true

  tasks:

    - name: Installer nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Déployer le template
      template:
        src: index.html.j2
        dest: /var/www/html/index.html